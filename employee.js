var employee = (function() {
	this.name = getName();
	this.age = getAge();
  this.salary = getSalary();
  function getName(name) {
    return name;
  }
  function getAge(age) {
    return age;
  }
  function getSalary(salary) {
    return salary;
  }
  return {
    
  }());